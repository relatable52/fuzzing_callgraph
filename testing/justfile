set dotenv-load := true
set shell := ["bash", "-uc"]

run_select:
    while read program; do \
        just \
        --dotenv-path "$program/.env"\
        projects/build_jar; \
    done < projects/to_run.txt
